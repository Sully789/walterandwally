<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content" android:layout_height="wrap_content">

    <ImageView
        android:id="@+id/char_image"
        android:layout_width="150dp"
        android:layout_height="150dp"
        android:layout_centerHorizontal="true" />

    <ScrollView
        android:id="@+id/char_scrollview"
        android:layout_width="wrap_content"
        android:layout_below="@+id/char_image"
        android:layout_height="wrap_content">

        <TextView
            android:text="@+id/char_textview"
            android:id="@+id/char_textview"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content" />
    </ScrollView>

    <Button
        android:id="@+id/char_back_to_lore"
        android:layout_below="@id/char_scrollview"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:text="@string/back_to_lore" />

</RelativeLayout>